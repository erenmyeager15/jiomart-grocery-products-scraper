{
  "title": "JioMart Grocery Scraper Input",
  "description": "Search JioMart for public grocery and product catalog data, prices, discounts, stock status, images, and URLs. For a low-cost test, use one query, maxResults 5-10, maxPagesPerQuery 1, and India residential proxy.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchQueries": {
      "title": "Search queries",
      "description": "One or more JioMart product searches. Start with one query for a low-cost test. Duplicate products are removed across the run.",
      "type": "array",
      "editor": "stringList",
      "default": ["rice"],
      "prefill": ["rice", "milk"]
    },
    "locationName": {
      "title": "Location label",
      "description": "Human-readable location label. JioMart may refine the final detected city, state, and pincode inside the browser session.",
      "type": "string",
      "editor": "textfield",
      "default": "Mumbai"
    },
    "latitude": {
      "title": "Latitude",
      "description": "Delivery-area latitude used for JioMart's location-aware catalog.",
      "type": "number",
      "editor": "number",
      "minimum": -90,
      "maximum": 90,
      "default": 19.076
    },
    "longitude": {
      "title": "Longitude",
      "description": "Delivery-area longitude used for JioMart's location-aware catalog.",
      "type": "number",
      "editor": "number",
      "minimum": -180,
      "maximum": 180,
      "default": 72.8777
    },
    "brands": {
      "title": "Brand filter",
      "description": "Optional exact brand names. Matching is case-insensitive.",
      "type": "array",
      "editor": "stringList",
      "default": []
    },
    "categories": {
      "title": "Category filter",
      "description": "Optional category text filters matched against category, subcategory, and leaf category.",
      "type": "array",
      "editor": "stringList",
      "default": []
    },
    "inStockOnly": {
      "title": "In-stock products only",
      "description": "Exclude products that are not sellable in the detected JioMart store.",
      "type": "boolean",
      "editor": "checkbox",
      "default": false
    },
    "minPrice": {
      "title": "Minimum price (INR)",
      "description": "Only save products at or above this price.",
      "type": "number",
      "editor": "number",
      "minimum": 0,
      "default": 0
    },
    "maxPrice": {
      "title": "Maximum price (INR)",
      "description": "Only save products at or below this price.",
      "type": "number",
      "editor": "number",
      "minimum": 0,
      "default": 1000000
    },
    "maxResults": {
      "title": "Maximum products",
      "description": "Maximum unique products saved across the complete run. Use 5-10 for test runs. Use this field, not maxProducts.",
      "type": "integer",
      "editor": "number",
      "minimum": 1,
      "maximum": 500,
      "default": 10
    },
    "maxPagesPerQuery": {
      "title": "Pages per query",
      "description": "Maximum JioMart result payloads processed for each search query. Use 1 for test runs. Use this field, not pagesPerQuery.",
      "type": "integer",
      "editor": "number",
      "minimum": 1,
      "maximum": 40,
      "default": 1
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "description": "India residential proxies are recommended for regional inventory and pricing. Proxy traffic is billed as platform usage.",
      "type": "object",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"],
        "apifyProxyCountry": "IN"
      },
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"],
        "apifyProxyCountry": "IN"
      }
    }
  }
}
